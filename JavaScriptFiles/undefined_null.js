/*
we have declared a variable, value is not assigned. when tried to access
the value in is undefined.

once we assign a value to it, then we can access that value.
*/

let a;
console.log(a); //undefined

a=10;
console.log(a); //10

/*

const a;
console.log(a); gives error.

const values are to be initialized when declared. else we get error 
as const variables once declared cannot be updated.

*/

a = null;
console.log(a); // oblect is assigned with null, that means absebce of object.